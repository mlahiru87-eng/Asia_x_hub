import { Analytics } from '@vercel/analytics/next'
import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import './globals.css'

const geistSans = Geist({ variable: '--font-geist-sans', subsets: ['latin'] })
const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
})

export const metadata: Metadata = {
  title: 'Asia X Hub — Trending Videos & Exclusive Entertainment',
  description:
    'Watch trending videos, exclusive entertainment content and daily updates on Asia X Hub. Join our Telegram community for instant access to fresh releases.',
  generator: 'v0.app',
  keywords: [
    'Asia X Hub',
    'trending videos',
    'entertainment',
    'streaming',
    'exclusive content',
    'telegram channel',
  ],
  openGraph: {
    title: 'Asia X Hub — Trending Videos & Exclusive Entertainment',
    description:
      'Watch trending videos, exclusive entertainment content and daily updates on Asia X Hub.',
    type: 'website',
  },
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export const viewport = {
  themeColor: '#0a0a0f',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className={`dark ${geistSans.variable} ${geistMono.variable}`}>
      <body className="bg-background font-sans antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
